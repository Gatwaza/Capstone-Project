<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  ios/Runner/Info.plist
  Merge these keys into your existing Info.plist file
  (do not replace the entire file — Flutter generates the base).
-->
<plist version="1.0">
<dict>
    <!-- Camera usage descriptions (REQUIRED for App Store) -->
    <key>NSCameraUsageDescription</key>
    <string>CPR-AI Coach uses your camera to track body position and provide real-time CPR coaching feedback.</string>

    <key>NSMicrophoneUsageDescription</key>
    <string>Microphone access is requested by the camera framework but audio is not recorded.</string>

    <!-- Prevent screen sleep during CPR session -->
    <key>UIRequiresPersistentWiFi</key>
    <false/>

    <!-- Portrait only -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
    </array>
</dict>
</plist>
