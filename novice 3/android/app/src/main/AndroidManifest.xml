<?xml version="1.0" encoding="utf-8"?>
<!-- Novice — CPR-AI Coach -->
<!-- GNU General Public License v3.0 -->
<!-- Copyright (C) 2024 Jean Robert Gatwaza — African Leadership University -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Camera: required for pose estimation -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Audio: required for TTS coaching prompts -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Camera feature declaration: not required (degrades gracefully) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <application
        android:label="Novice"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- TFLite requires this meta-data for GPU delegate -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="pose"/>

    </application>
</manifest>
