# Novice — Kinyarwanda TTS requirements
# GNU General Public License v3.0
# Copyright (C) 2024 Jean Robert Gatwaza — African Leadership University
#
# Install separately from the main ML pipeline requirements:
#   pip install -r src/tts/tts_requirements.txt
#
# Kept separate because transformers/torch add ~2 GB and are only needed
# for TTS server / pre-baking audio — not for BiLSTM training.

# ── Model loading ──────────────────────────────────────────
transformers>=4.40.0        # AutoModel, AutoProcessor
huggingface-hub>=0.23.0     # model download + caching

# ── PyTorch (Apple Silicon) ────────────────────────────────
# For M1 Max: torch with MPS (Metal Performance Shaders) backend
torch>=2.2.0               # includes MPS support for Apple Silicon

# ── Audio I/O ──────────────────────────────────────────────
soundfile>=0.12.1           # write WAV bytes in memory
numpy>=1.26.0               # audio array manipulation

# ── HTTP server ────────────────────────────────────────────
flask>=3.0.0                # local TTS server (kinyarwanda_tts_server.py)

# ── Optional: accelerate speeds up model loading ──────────
accelerate>=0.29.0
